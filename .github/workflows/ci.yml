name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: api/requirements.txt

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      # Python backend
      - name: Install Python dependencies
        run: pip install -r api/requirements.txt

      - name: Lint Python (ruff)
        run: pip install ruff==0.15.17 && ruff check api/ ml/ tests/ benchmarks/

      - name: Type check Python (mypy)
        run: pip install mypy==2.1.0 && mypy

      - name: Run pytest
        run: pytest tests/ -v

      # The trained model is committed (see ml/TRAINING.md); make sure code
      # changes never break unpickling or the ml_model prediction path.
      - name: Verify committed ML model artifact
        run: python scripts/verify_model.py

      # Frontend
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Type check frontend
        working-directory: frontend
        run: npx tsc --noEmit

      - name: Run frontend tests (Vitest)
        working-directory: frontend
        run: npm test

      - name: Build frontend
        working-directory: frontend
        run: npm run build

  docker:
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t stock-analyzer:test .

      - name: Smoke test
        run: |
          docker run -d --name test -p 8080:8080 stock-analyzer:test
          for i in {1..15}; do
            if curl -fs http://localhost:8080/api/health | grep -q '"status"'; then
              echo "Health check passed after ${i}s"
              break
            fi
            sleep 1
          done
          curl -f http://localhost:8080/api/health
          curl -fs http://localhost:8080/ | grep -q "root"
          docker stop test
